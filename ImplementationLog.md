# Implementation Log

| Date (UTC) | Summary | Technical / Architectural Notes |
|------------|---------|----------------------------------|
| 2025-09-26 | Created initial `AGENTS.md` tailored for GitHub Copilot usage. | Captured collaboration workflow, tooling standards (uv, FastAPI, React `assistant-ui`), Terraform structuring rules, and documentation expectations aligned with `PRD.md` and `SolutionDesign.md`. |
| 2025-09-26 | Populated component README files with solution-aligned descriptions. | Documented facilitator, product finder, frontend, MCP tools, and Terraform scope per architecture to guide future implementation work. |
| 2025-09-26 | Implemented Terraform root and module scaffolding for networking, application, foundry agents, and jump access. | Provisioned VNet with firewall/DNS, ACA + Cosmos (vector index), Azure AI Foundry Agent Service via azapi, and Bastion + VM jump host wired together through module outputs/inputs. |
| 2025-09-27 | Refined Terraform networking, Foundry, and jump modules per new architecture direction. | Subnets now use programmatic `cidrsubnet` allocation, the jump host runs Windows 11 with Bastion-accessible RDP, and the Foundry module provisions only the Azure AI Foundry account, private endpoint, and GPT-5 deployment. |
| 2025-09-27 | Migrated Foundry provisioning to azapi resources. | Azure AI Foundry account and GPT-5 deployment now use azapi providers, reusing the resource group ID for parent references while keeping private endpoints and outputs intact. |
| 2025-09-27 | Refactored application module layout. | Split the application Terraform into focused files for monitoring, container apps, Cosmos DB, and ACR/private endpoints to improve maintainability. |
| 2025-09-27 | Resolved deployment blockers and completed terraform apply. | Removed stale Foundry private endpoint, recreated container app environment with ACR-hosted images, reintroduced Bastion NSG, and verified full stack provisions successfully via `terraform apply -auto-approve`. |
| 2025-09-28 | Hardcoded Azure AI Foundry deployments for GPT-5 family plus GPT-4.1 with latest control-plane API. | Updated Foundry module to use API version `2025-06-01`, fixed the account SKU to `S0`, provisioned gpt-5, gpt-5-mini, and gpt-4.1 deployments at 50 PTUs each, and streamlined outputs to surface all deployment IDs. |
| 2025-09-29 | Pointed container apps to Azure-hosted sample images for early bring-up. | Defaulted both backend and frontend container apps to `mcr.microsoft.com/azuredocs/containerapps-helloworld:latest` and kept registry attachment conditional so the apps can start before custom images are available. |
| 2025-09-30 | Upgraded Foundry provisioning to the Standard enterprise template with VNet injection. | Added storage, Cosmos DB, and Azure AI Search dependencies with private endpoints, enabled agent subnet network injection, wired AI Foundry project/capability host, and replicated RBAC and data-plane role assignments from the Microsoft reference deployment. |
| 2025-09-30 | Aligned network CIDRs and datastore naming for Standard Foundry requirements. | Added a dedicated 172.21.32.0/24 subnet (with supporting VNet address range) for the Foundry agent network injection while leaving existing 10.50.0.0/16 allocations intact, and gave the Foundry Cosmos DB account a unique `*-agent` suffix to avoid collisions with the application database. |
| 2025-09-30 | Added Windows developer bootstrap script. | Created `scripts/install-dev-tools.ps1` to install VS Code, Terraform, Azure CLI, Git, and GitHub CLI via winget on Windows 11. |
| 2025-09-30 | Migrated Azure Firewall from classic rules to Firewall Policy with organized rule collection groups. | Replaced legacy `azurerm_firewall_application_rule_collection` resources with modern `azurerm_firewall_policy` and `azurerm_firewall_policy_rule_collection_group` resources. Organized rules into separate files: `firewall_policy.tf`, `firewall_rules_windows.tf` (Windows activation, updates, WinGet), `firewall_rules_devtools.tf` (Azure CLI, GitHub, Terraform, VS Code), `firewall_rules_python.tf` (pip, PyPI, uv/Astral), and `firewall_rules_web.tf` (search engines, Tavily API, Microsoft Learn, MCR, CDNs). Covers all Windows essential services, developer tooling, Python package managers, and common web services. |
| 2025-10-01 | Migrated jump host from Windows to Ubuntu 24.04 Linux with Bastion Standard SKU for VS Code Remote SSH support. | Replaced Windows 11 VM with Ubuntu 24.04 LTS, upgraded Azure Bastion from Basic to Standard SKU to enable SSH tunneling and native integration, configured cloud-init to install Azure CLI, Terraform, Git, Python 3, pip, and uv package manager. Added SSH configuration for TCP forwarding to support VS Code Remote SSH via Azure Bastion tunnel mode. Created comprehensive documentation in `modules/jump/README.md` with VS Code Remote SSH setup instructions using `az network bastion tunnel` and ProxyCommand. |
| 2025-10-02 | Replaced Windows firewall rules with Ubuntu/Linux-specific rules and enhanced devtools coverage. | Removed `firewall_rules_windows.tf` and created `firewall_rules_ubuntu.tf` with Ubuntu-specific endpoints: apt repositories (archive.ubuntu.com, security.ubuntu.com), package management (snapcraft.io, launchpad.net), system services (ntp.ubuntu.com, changelogs.ubuntu.com). Updated devtools firewall rules to include Azure CLI installation script endpoint (aka.ms), enhanced GitHub releases coverage, and added explicit support for HashiCorp GPG keys and Astral.sh (uv installer). All firewall rules now align with Ubuntu 24.04 jump host requirements. |
| 2025-10-01 | Replaced Windows firewall rules with Ubuntu/Linux-specific firewall rules. | Renamed `firewall_rules_windows.tf` to `firewall_rules_ubuntu.tf` and replaced Windows Update/WinGet rules with Ubuntu apt repositories, certificate validation (OCSP), and NTP time synchronization endpoints. Updated developer tools rules to include Azure CLI installation endpoints (aka.ms, packages.microsoft.com, azurecliprod.blob.core.windows.net). Ensures jump host can perform system updates, package installations, and developer tooling setup. |
| 2025-10-02 | Updated SSH tunnel command output format to use IP address instead of resource ID. | Changed `tunnel_command_example` output in `modules/jump/outputs.tf` from `--target-resource-id` parameter (using VM resource ID) to `--target-ip-address` parameter (using NIC private IP: 172.21.16.4). This aligns with user's working command format for `az network bastion tunnel` and provides more direct connectivity. Applied via `terraform apply -auto-approve` with plan showing 0 added, 2 changed (container app drift), 0 destroyed. |
| 2025-10-02 | Added GitHub Copilot proxy endpoint to developer tools firewall rules. | Added `copilot-proxy.githubusercontent.com` to the github-repositories-cli rule in `firewall_rules_devtools.tf` to enable GitHub Copilot service connectivity from the jump host. Applied via targeted deployment: `terraform apply -target=module.networking.azurerm_firewall_policy_rule_collection_group.developer_tools -refresh=false -auto-approve` (0 added, 1 changed, 0 destroyed in ~3 minutes). All other requested FQDNs (github.com, api.github.com, objects.githubusercontent.com, raw.githubusercontent.com, marketplace.visualstudio.com, update.code.visualstudio.com) were already present. |
| 2025-10-02 | Implemented Products MCP Server with FastMCP and Azure Cosmos DB integration. | Created complete MCP server implementation using FastMCP with five tools: `get_user_profile`, `vector_search_products`, `get_pricing`, `get_stock`, and `rank_candidates_tool`. Implemented Pydantic models for request/response validation matching SolutionDesign.md contracts, Azure Cosmos DB client with DefaultAzureCredential support for Managed Identity authentication, heuristic ranking algorithm with weighted scoring (0.55 similarity + 0.15 popularity + 0.15 price + 0.15 availability), and comprehensive settings management via pydantic-settings with .env file support. Added FastMCP client-based testing utility with both in-memory and external server test modes. Updated dependencies in pyproject.toml: fastmcp>=2.4.0, azure-cosmos>=4.8.0, azure-identity>=1.19.0, python-dotenv>=1.0.0, pydantic>=2.0.0, pytest>=8.0.0. |
| 2025-10-02 | Configured Cosmos DB RBAC for backend container apps and jump VM. | Created `terraform/modules/application/rbac.tf` with Cosmos DB Data Contributor role assignments using built-in role ID `00000000-0000-0000-0000-000000000002`. Granted backend container app user-assigned identity full data plane access to Cosmos DB for production MCP server operations. Added conditional role assignment for jump VM system-assigned identity to enable testing from the jump host. Updated application module variables to accept `jump_vm_principal_id` parameter and wired it through root main.tf using `module.jump.jump_host_identity_principal_id` output. Frontend container app intentionally has no direct Cosmos access as it communicates through backend API only. All authentication uses Azure RBAC (no connection strings). |
| 2025-10-02 | Removed hardcoded role assignment names from Foundry module to prevent conflicts. | Removed `name` parameter from all six `azurerm_role_assignment` resources in `modules/foundry/main.tf`: `cosmos_operator`, `storage_blob_contributor`, `search_index_data_contributor`, `search_service_contributor`, `storage_blob_owner_restricted`, and `jump_host_ai_user`. Azure now auto-generates unique GUID names for each role assignment, preventing 409 Conflict errors during re-deployments. Imported existing `jump_host_ai_user` role assignment (ID: ddda9dcf-5b96-49bc-af2c-8e2429f1ea46) into Terraform state. Applied successfully: 0 added, 2 changed (container app drift cleanup), 0 destroyed. |
| 2025-10-02 | Replaced built-in Cosmos DB RBAC role with custom role definition for proper scope support. | Created custom `azurerm_cosmosdb_sql_role_definition` resource in `modules/application/rbac.tf` to replace the built-in role (`00000000-0000-0000-0000-000000000002`). The built-in role has limitations with database-level scoping. Custom role provides identical data actions (`Microsoft.DocumentDB/databaseAccounts/readMetadata`, `Microsoft.DocumentDB/databaseAccounts/sqlDatabases/containers/*`, `Microsoft.DocumentDB/databaseAccounts/sqlDatabases/containers/items/*`) with proper scope flexibility at the account level. Updated both `backend` and `jump_vm` role assignments to reference the custom role definition. This ensures proper data plane access for backend container app managed identity and jump host testing. |
| 2025-10-02 | Added SSH public key support to jump VM alongside password authentication. | Added `admin_ssh_public_key` variable to jump module and root `variables.tf` allowing users to configure SSH key authentication via `secrets.auto.tfvars`. Jump VM now supports both SSH key and password authentication simultaneously (`disable_password_authentication = false` with optional `admin_ssh_key` block using dynamic block pattern). Changed jump VM role assignment pattern from `count` with `null` checks to using empty string defaults with local variables (`local.enable_jump_vm_access`) to avoid Terraform dependency cycle errors. Updated `modules/application/variables.tf` and `modules/foundry/variables.tf` to use empty string (`""`) as default instead of `null` for `jump_vm_principal_id` and `jump_host_identity_principal_id`. Created `terraform/DEPLOYMENT_GUIDE.md` documenting two-stage deployment approach using `-target` flag to handle jump VM recreation scenarios where principal_id is unknown at plan time. |

```

````

```
