# Implementation Log

| Date (UTC) | Summary | Technical / Architectural Notes |
|------------|---------|----------------------------------|
| 2025-09-26 | Created initial `AGENTS.md` tailored for GitHub Copilot usage. | Captured collaboration workflow, tooling standards (uv, FastAPI, React `assistant-ui`), Terraform structuring rules, and documentation expectations aligned with `PRD.md` and `SolutionDesign.md`. |
| 2025-09-26 | Populated component README files with solution-aligned descriptions. | Documented facilitator, product finder, frontend, MCP tools, and Terraform scope per architecture to guide future implementation work. |
| 2025-09-26 | Implemented Terraform root and module scaffolding for networking, application, foundry agents, and jump access. | Provisioned VNet with firewall/DNS, ACA + Cosmos (vector index), Azure AI Foundry Agent Service via azapi, and Bastion + VM jump host wired together through module outputs/inputs. |
| 2025-09-27 | Refined Terraform networking, Foundry, and jump modules per new architecture direction. | Subnets now use programmatic `cidrsubnet` allocation, the jump host runs Windows 11 with Bastion-accessible RDP, and the Foundry module provisions only the Azure AI Foundry account, private endpoint, and GPT-5 deployment. |
| 2025-09-27 | Migrated Foundry provisioning to azapi resources. | Azure AI Foundry account and GPT-5 deployment now use azapi providers, reusing the resource group ID for parent references while keeping private endpoints and outputs intact. |
| 2025-09-27 | Refactored application module layout. | Split the application Terraform into focused files for monitoring, container apps, Cosmos DB, and ACR/private endpoints to improve maintainability. |
| 2025-09-27 | Resolved deployment blockers and completed terraform apply. | Removed stale Foundry private endpoint, recreated container app environment with ACR-hosted images, reintroduced Bastion NSG, and verified full stack provisions successfully via `terraform apply -auto-approve`. |
| 2025-09-28 | Hardcoded Azure AI Foundry deployments for GPT-5 family plus GPT-4.1 with latest control-plane API. | Updated Foundry module to use API version `2025-06-01`, fixed the account SKU to `S0`, provisioned gpt-5, gpt-5-mini, and gpt-4.1 deployments at 50 PTUs each, and streamlined outputs to surface all deployment IDs. |
| 2025-09-29 | Pointed container apps to Azure-hosted sample images for early bring-up. | Defaulted both backend and frontend container apps to `mcr.microsoft.com/azuredocs/containerapps-helloworld:latest` and kept registry attachment conditional so the apps can start before custom images are available. |
| 2025-09-30 | Upgraded Foundry provisioning to the Standard enterprise template with VNet injection. | Added storage, Cosmos DB, and Azure AI Search dependencies with private endpoints, enabled agent subnet network injection, wired AI Foundry project/capability host, and replicated RBAC and data-plane role assignments from the Microsoft reference deployment. |
| 2025-09-30 | Aligned network CIDRs and datastore naming for Standard Foundry requirements. | Added a dedicated 172.21.32.0/24 subnet (with supporting VNet address range) for the Foundry agent network injection while leaving existing 10.50.0.0/16 allocations intact, and gave the Foundry Cosmos DB account a unique `*-agent` suffix to avoid collisions with the application database. |
| 2025-09-30 | Added Windows developer bootstrap script. | Created `scripts/install-dev-tools.ps1` to install VS Code, Terraform, Azure CLI, Git, and GitHub CLI via winget on Windows 11. |
| 2025-09-30 | Migrated Azure Firewall from classic rules to Firewall Policy with organized rule collection groups. | Replaced legacy `azurerm_firewall_application_rule_collection` resources with modern `azurerm_firewall_policy` and `azurerm_firewall_policy_rule_collection_group` resources. Organized rules into separate files: `firewall_policy.tf`, `firewall_rules_windows.tf` (Windows activation, updates, WinGet), `firewall_rules_devtools.tf` (Azure CLI, GitHub, Terraform, VS Code), `firewall_rules_python.tf` (pip, PyPI, uv/Astral), and `firewall_rules_web.tf` (search engines, Tavily API, Microsoft Learn, MCR, CDNs). Covers all Windows essential services, developer tooling, Python package managers, and common web services. |
| 2025-10-01 | Migrated jump host from Windows to Ubuntu 24.04 Linux with Bastion Standard SKU for VS Code Remote SSH support. | Replaced Windows 11 VM with Ubuntu 24.04 LTS, upgraded Azure Bastion from Basic to Standard SKU to enable SSH tunneling and native integration, configured cloud-init to install Azure CLI, Terraform, Git, Python 3, pip, and uv package manager. Added SSH configuration for TCP forwarding to support VS Code Remote SSH via Azure Bastion tunnel mode. Created comprehensive documentation in `modules/jump/README.md` with VS Code Remote SSH setup instructions using `az network bastion tunnel` and ProxyCommand. |
| 2025-10-02 | Replaced Windows firewall rules with Ubuntu/Linux-specific rules and enhanced devtools coverage. | Removed `firewall_rules_windows.tf` and created `firewall_rules_ubuntu.tf` with Ubuntu-specific endpoints: apt repositories (archive.ubuntu.com, security.ubuntu.com), package management (snapcraft.io, launchpad.net), system services (ntp.ubuntu.com, changelogs.ubuntu.com). Updated devtools firewall rules to include Azure CLI installation script endpoint (aka.ms), enhanced GitHub releases coverage, and added explicit support for HashiCorp GPG keys and Astral.sh (uv installer). All firewall rules now align with Ubuntu 24.04 jump host requirements. |
| 2025-10-01 | Replaced Windows firewall rules with Ubuntu/Linux-specific firewall rules. | Renamed `firewall_rules_windows.tf` to `firewall_rules_ubuntu.tf` and replaced Windows Update/WinGet rules with Ubuntu apt repositories, certificate validation (OCSP), and NTP time synchronization endpoints. Updated developer tools rules to include Azure CLI installation endpoints (aka.ms, packages.microsoft.com, azurecliprod.blob.core.windows.net). Ensures jump host can perform system updates, package installations, and developer tooling setup. |
| 2025-10-02 | Updated SSH tunnel command output format to use IP address instead of resource ID. | Changed `tunnel_command_example` output in `modules/jump/outputs.tf` from `--target-resource-id` parameter (using VM resource ID) to `--target-ip-address` parameter (using NIC private IP: 172.21.16.4). This aligns with user's working command format for `az network bastion tunnel` and provides more direct connectivity. Applied via `terraform apply -auto-approve` with plan showing 0 added, 2 changed (container app drift), 0 destroyed. |
| 2025-10-02 | Added GitHub Copilot proxy endpoint to developer tools firewall rules. | Added `copilot-proxy.githubusercontent.com` to the github-repositories-cli rule in `firewall_rules_devtools.tf` to enable GitHub Copilot service connectivity from the jump host. Applied via targeted deployment: `terraform apply -target=module.networking.azurerm_firewall_policy_rule_collection_group.developer_tools -refresh=false -auto-approve` (0 added, 1 changed, 0 destroyed in ~3 minutes). All other requested FQDNs (github.com, api.github.com, objects.githubusercontent.com, raw.githubusercontent.com, marketplace.visualstudio.com, update.code.visualstudio.com) were already present. |
| 2025-10-02 | Implemented Products MCP Server with FastMCP and Azure Cosmos DB integration. | Created complete MCP server implementation using FastMCP with five tools: `get_user_profile`, `vector_search_products`, `get_pricing`, `get_stock`, and `rank_candidates_tool`. Implemented Pydantic models for request/response validation matching SolutionDesign.md contracts, Azure Cosmos DB client with DefaultAzureCredential support for Managed Identity authentication, heuristic ranking algorithm with weighted scoring (0.55 similarity + 0.15 popularity + 0.15 price + 0.15 availability), and comprehensive settings management via pydantic-settings with .env file support. Added FastMCP client-based testing utility with both in-memory and external server test modes. Updated dependencies in pyproject.toml: fastmcp>=2.4.0, azure-cosmos>=4.8.0, azure-identity>=1.19.0, python-dotenv>=1.0.0, pydantic>=2.0.0, pytest>=8.0.0. |
| 2025-10-02 | Configured Cosmos DB RBAC for backend container apps and jump VM. | Created `terraform/modules/application/rbac.tf` with Cosmos DB Data Contributor role assignments using built-in role ID `00000000-0000-0000-0000-000000000002`. Granted backend container app user-assigned identity full data plane access to Cosmos DB for production MCP server operations. Added conditional role assignment for jump VM system-assigned identity to enable testing from the jump host. Updated application module variables to accept `jump_vm_principal_id` parameter and wired it through root main.tf using `module.jump.jump_host_identity_principal_id` output. Frontend container app intentionally has no direct Cosmos access as it communicates through backend API only. All authentication uses Azure RBAC (no connection strings). |
| 2025-10-02 | Removed hardcoded role assignment names from Foundry module to prevent conflicts. | Removed `name` parameter from all six `azurerm_role_assignment` resources in `modules/foundry/main.tf`: `cosmos_operator`, `storage_blob_contributor`, `search_index_data_contributor`, `search_service_contributor`, `storage_blob_owner_restricted`, and `jump_host_ai_user`. Azure now auto-generates unique GUID names for each role assignment, preventing 409 Conflict errors during re-deployments. Imported existing `jump_host_ai_user` role assignment (ID: ddda9dcf-5b96-49bc-af2c-8e2429f1ea46) into Terraform state. Applied successfully: 0 added, 2 changed (container app drift cleanup), 0 destroyed. |
| 2025-10-02 | Replaced built-in Cosmos DB RBAC role with custom role definition for proper scope support. | Created custom `azurerm_cosmosdb_sql_role_definition` resource in `modules/application/rbac.tf` to replace the built-in role (`00000000-0000-0000-0000-000000000002`). The built-in role has limitations with database-level scoping. Custom role provides identical data actions (`Microsoft.DocumentDB/databaseAccounts/readMetadata`, `Microsoft.DocumentDB/databaseAccounts/sqlDatabases/containers/*`, `Microsoft.DocumentDB/databaseAccounts/sqlDatabases/containers/items/*`) with proper scope flexibility at the account level. Updated both `backend` and `jump_vm` role assignments to reference the custom role definition. This ensures proper data plane access for backend container app managed identity and jump host testing. |
| 2025-10-02 | Added SSH public key support to jump VM alongside password authentication. | Added `admin_ssh_public_key` variable to jump module and root `variables.tf` allowing users to configure SSH key authentication via `secrets.auto.tfvars`. Jump VM now supports both SSH key and password authentication simultaneously (`disable_password_authentication = false` with optional `admin_ssh_key` block using dynamic block pattern). Changed jump VM role assignment pattern from `count` with `null` checks to using empty string defaults with local variables (`local.enable_jump_vm_access`) to avoid Terraform dependency cycle errors. Updated `modules/application/variables.tf` and `modules/foundry/variables.tf` to use empty string (`""`) as default instead of `null` for `jump_vm_principal_id` and `jump_host_identity_principal_id`. Created `terraform/DEPLOYMENT_GUIDE.md` documenting two-stage deployment approach using `-target` flag to handle jump VM recreation scenarios where principal_id is unknown at plan time. |
| 2025-10-03 | Created Azure Container Registry remote build script for MCP server. | Built Python-based deployment script `deploy/build_and_push_mcp.py` using Azure CLI's `az acr build` for serverless container image builds. Script loads ACR_NAME from `.env` file, validates Azure CLI installation and authentication, uploads build context from `src/tools/products_mcp/` to Azure, and triggers remote build with linux/amd64 platform targeting. Benefits include no local Docker daemon requirement, faster builds using Azure infrastructure, and automatic push to ACR. Created `deploy/README.md` documenting usage and prerequisites. Script executable via `uv run build_and_push_mcp.py` with colored console output for progress tracking. |
| 2025-10-03 | Deployed MCP server to Azure Container Apps with private endpoint and split container app Terraform files. | Split monolithic `container_apps.tf` into focused files: `container_apps_shared.tf` (environment, identities, locals), `container_apps_backend.tf`, `container_apps_frontend.tf`, and `container_apps_mcp.tf`. Added `mcp_image` variable to application module with smart default to `{ACR}/products-mcp:latest`. Created MCP container app with 0.5 CPU / 1Gi memory, internal-only ingress on port 8080, environment variables for Cosmos DB (endpoint, database, containers) and embeddings configuration, 1-3 replica autoscaling, and user-assigned managed identity. Configured private endpoint for MCP server on `snet-pes` subnet with Container App Environment subresource and private DNS zone integration for internal access. Added ACR pull RBAC for MCP identity and Cosmos DB Data Contributor role assignment. Exposed `mcp_container_app_fqdn` in Terraform outputs for testing. Created comprehensive deployment test file `src/tools/products_mcp/test_deployment.py` using FastMCP client with SSE transport to validate deployed MCP server from jump host. Test suite validates server health, tool registration, and all five MCP tools with mock data. Updated pytest configuration to exclude deployment tests from default `uv run pytest` execution (run explicitly with `pytest test_deployment.py`). Enhanced MCP README with deployment testing instructions and updated root Terraform outputs with MCP testing guidance for jump host users. |
| 2025-10-03 | Fixed MCP container embeddings configuration to use correct dimensions and Azure AI Foundry endpoint. | Updated MCP container app to use 2048-dimensional embeddings (matching seed data and Cosmos DB vector configuration) instead of 1536. Changed environment variable names to match config.py expectations: `EMBEDDINGS_ENDPOINT` (instead of AZURE_OPENAI_ENDPOINT), `EMBEDDINGS_DEPLOYMENT` (instead of EMBEDDING_MODEL), and `EMBEDDINGS_DIMENSIONS`. Added `embeddings_endpoint` variable to application module and wired it from `module.foundry.cognitive_account_endpoint` in root main.tf. Embeddings endpoint now correctly points to Azure AI Foundry account (e.g., https://dafcpld.cognitiveservices.azure.com) where text-embedding-3-large model is deployed. This ensures MCP server uses the proper embedding model deployed in Azure AI Foundry for vector search operations. |
| 2025-10-03 | Configured MCP server managed identity for Cosmos DB and Azure OpenAI access. | Added `AZURE_CLIENT_ID` environment variable to MCP container app pointing to the user-assigned managed identity's client ID, ensuring DefaultAzureCredential explicitly uses the correct identity in multi-identity scenarios. Verified Cosmos DB RBAC assignment already exists in `rbac.tf` granting MCP identity the custom Data Contributor role for full data plane access. Added `mcp_identity_principal_id` variable to foundry module and created `azurerm_role_assignment.mcp_openai_user` granting "Cognitive Services OpenAI User" role on the Azure AI Foundry account, enabling the MCP server to call text-embedding-3-large deployment for vector embedding generation. Wired MCP principal ID from application module output through root main.tf to foundry module. Added `mcp_managed_identity_principal_id` output to application module for RBAC assignments. MCP server now has complete managed identity configuration for both Cosmos DB data access and Azure OpenAI embeddings generation without any connection strings or keys. |
| 2025-10-03 | Added lifecycle ignore_changes to jump VM cloud-init configuration. | Added `lifecycle { ignore_changes = [custom_data] }` block to `azurerm_linux_virtual_machine.jump` resource in `modules/jump/main.tf`. Terraform will now apply cloud-init configuration only during initial VM creation and ignore subsequent changes to `cloud-init.yaml` template, preventing unnecessary VM recreation when cloud-init configuration is updated. |
| 2025-10-03 | Simplified Foundry module by removing conditional role assignments and time_sleep resources. | Removed `local.enable_jump_host_access` and `local.enable_mcp_access` conditional logic from `modules/foundry/main.tf`. Role assignments for jump host (Azure AI User, Cognitive Services OpenAI User) and MCP server (Cognitive Services OpenAI User) are now always created since these identities are always provisioned. Removed all three `time_sleep` resources (`wait_project_identity`, `wait_rbac`, `wait_capability_host`) as explicit `depends_on` chains are sufficient for proper resource ordering. Updated capability_host to depend directly on role assignments instead of time_sleep. Updated variables in `modules/foundry/variables.tf` to make `jump_host_identity_principal_id` and `mcp_identity_principal_id` required (removed default empty string values). Terraform plan shows three time_sleep resources will be destroyed and two role assignments moved from count-based to direct resources (no actual change, just state refactoring). This simplifies the module by removing unnecessary wait times and conditional complexity while maintaining proper dependency ordering. |
| 2025-10-03 | Added region-specific Azure Container Apps private DNS zone to networking module. | Added Container Apps private DNS zone with region-aware name construction in `modules/networking/main.tf`. Created `local.private_dns_zones` map that merges static DNS zones from variables with dynamically constructed Container Apps zone (`privatelink.{location}.azurecontainerapps.io`). For swedencentral region, this creates `privatelink.swedencentral.azurecontainerapps.io`. Updated both `azurerm_private_dns_zone` and `azurerm_private_dns_zone_virtual_network_link` resources to use the merged locals map instead of variable directly. This allows MCP container app private endpoint to reference `var.private_dns_zone_ids["aca"]` for proper private connectivity. DNS zone naming follows Azure documentation pattern for Container Apps private endpoints with managedEnvironments subresource. |
| 2025-10-03 | Configured Container App Environment with private endpoint support by disabling public network access. | Added `azapi_update_resource.disable_public_access` in `container_apps_shared.tf` to set `publicNetworkAccess = "Disabled"` on the Container App Environment after creation. This is required to enable private endpoint connectivity per Azure Container Apps documentation. Created private endpoint resource `azurerm_private_endpoint.mcp` in `container_apps_mcp.tf` targeting the Container App Environment (not individual apps) with subresource `managedEnvironments`. Private endpoint connects to environment through `snet-pes` subnet with DNS zone group referencing the region-specific Container Apps private DNS zone. Added explicit `depends_on = [azapi_update_resource.disable_public_access]` to ensure public access is disabled before private endpoint creation. This configuration allows external VNets or services to securely connect to the Container Apps environment through Azure Private Link while maintaining VNet integration for the apps themselves via `infrastructure_subnet_id` and `internal_load_balancer_enabled`. |
| 2025-10-03 | Replaced Container App Environment with azapi_resource to properly configure publicNetworkAccess during creation. | Replaced post-deployment `azapi_update_resource` approach with proper resource creation using `azapi_resource` in `container_apps_shared.tf`. Discovered that `publicNetworkAccess` property is only available in preview API version `2025-02-02-preview` (not in stable 2024-03-01). Updated Container App Environment to use preview API with `publicNetworkAccess = "Disabled"` configured directly in the resource body properties. This prevents the need for post-creation updates and properly enables private endpoint support from the beginning. The azapi_resource uses HCL object syntax for body (not JSON-encoded string) and includes all required properties: appLogsConfiguration, vnetConfiguration, workloadProfiles, and publicNetworkAccess. All three container apps (backend, frontend, mcp) will be recreated to reference the new environment resource. Added data source for resource group to construct proper parent_id for azapi_resource. Private endpoint for MCP now depends directly on the environment creation rather than a separate update step. This aligns with Azure's recommendation to configure public network access settings during resource creation rather than via update operations. |
| 2025-10-03 | Fixed MCP container environment variables to match config.py field names. | Corrected environment variable names in `container_apps_mcp.tf` from Azure-prefixed names to match the exact field names expected by pydantic-settings in `config.py`: changed `AZURE_COSMOS_ENDPOINT` to `COSMOS_DB_ENDPOINT`, changed `AZURE_COSMOS_DATABASE` to `COSMOS_DB_NAME`, and removed `AZURE_COSMOS_CONTAINER` since container names are hardcoded in cosmos_client.py code (products, pricing, stock, user_profiles). This fixed the Pydantic validation error "Field required [type=missing, input_value=..., input_type=dict]" that was preventing the MCP container from starting. Applied via targeted deployment: `terraform apply -target='module.application.azurerm_container_app.mcp' -auto-approve` (0 added, 1 changed, 0 destroyed in ~36 seconds). MCP server should now start successfully with proper Cosmos DB configuration. |
| 2025-10-03 | Configured MCP server to always run in SSE HTTP mode instead of STDIO. | Updated `src/tools/products_mcp/main.py` to always call `mcp.run(transport="sse", host="0.0.0.0", port=8080)` instead of defaulting to STDIO mode. Since this MCP server is exclusively deployed to Azure Container Apps and accessed via HTTP, there's no need for conditional transport selection. The server now starts in SSE (Server-Sent Events) mode on port 8080, matching the Container App ingress configuration, enabling proper HTTP-based MCP protocol communication for Azure AI Foundry agents. |
| 2025-10-03 | Removed conditional jump VM Cosmos DB access logic to always grant access. | Removed `local.enable_jump_vm_access` conditional logic from `modules/application/rbac.tf` and eliminated the `count` parameter from `azurerm_cosmosdb_sql_role_assignment.jump_vm` resource. Jump VM will always receive Cosmos DB Data Contributor role assignment for testing purposes. Updated `modules/application/variables.tf` to make `jump_vm_principal_id` a required variable (removed default empty string). This simplifies the infrastructure by removing unnecessary conditional complexity - the jump VM is always provisioned, so it should always have data plane access for testing the MCP server and seeding Cosmos DB data. |
| 2025-10-03 | Created config_files Terraform module for automated .env file generation. | Implemented new Terraform module at `modules/config_files/` that uses `templatefile()` function and `local_file` resources to automatically generate environment configuration files (.env) and helper scripts from infrastructure values. The module generates: (1) `deploy/.env` with ACR name for build/push operations, (2) `src/tools/products_mcp/.env` with Cosmos DB and embeddings configuration, (3) `src/agents/facilitator/.env` and `src/agents/product_finder/.env` with AI Foundry project settings, (4) `scripts/connect_bastion_tunnel.ps1` with Bastion connectivity commands. Template files (`.tftpl`) are stored in `modules/config_files/templates/` and use `${variable}` syntax for Terraform interpolation. This eliminates manual copying of endpoints and resource names from Azure Portal or Terraform outputs, ensuring configuration stays synchronized with deployed infrastructure. Added comprehensive variables for MCP tuning (vector_top_k, similarity_weight, etc.) with sensible defaults that can be overridden in `secrets.auto.tfvars`. Module includes detailed README with usage examples and customization guidance. Added `repo_root` variable to specify absolute path to repository for file generation. Added `container_registry_name` and `cosmos_account_endpoint` outputs to application module to support config generation. |
| 2025-10-03 | Refactored config_files module to reduce variables and reference deployed resources. | Simplified config_files module by removing unnecessary variables. Now only `repo_root` and `log_level` are user-configurable variables. Model deployment names (`gpt-5`, `text-embedding-3-large`) are now referenced directly from deployed foundry module resources via new outputs (`llm_deployment_name`, `embeddings_deployment_name`). MCP tuning parameters (vector_top_k=20, min_similarity_threshold=0.5, embeddings_dimensions=2048, ranking weights) are hardcoded in the template file `products_mcp.env.tftpl` instead of being variables, as these are application constants that don't need infrastructure-level configuration. This makes the module more maintainable by keeping deployed resource references in Terraform and application constants in templates. Updated foundry module outputs to expose deployment names. Removed 11 unnecessary variables from root `variables.tf` (foundry_project_endpoint, model_deployment_name, embeddings_deployment, embeddings_dimensions, vector_top_k, min_similarity_threshold, similarity_weight, popularity_weight, price_weight, availability_weight). |

```

````

```
